{
  "name": "system-v",
  "version": "0.1.0",
  "private": true,
  "description": "Vignesh's personal portfolio — SYSTEM//V",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check"
  },
  "packageManager": "npm@11.6.0",
  "devDependencies": {
    "turbo": "^2.10.10"
  }
}
